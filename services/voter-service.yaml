apiVersion: v1
kind: Service
metadata:
  name: vote-service
  labels:
    app: vote
spec:
  selector:
    app: vote
  ports:
    - name: "vote-service"
      port: 8080
      targetPort: 8080
      nodePort: 30080
  type: NodePort